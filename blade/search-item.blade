li.wrapper.search-item(data-remood-value=result.uri)
  .container
    .left-col
      figure
        .layer
          img(src="#{result.artwork_url.replace(/-large\.jpg$/,'-t500x500.jpg')}")
        .layer
          i.play-control
    .right-col
      h1 #{result.title}
      h4
        small by
        | #{' ' + result.user.username}